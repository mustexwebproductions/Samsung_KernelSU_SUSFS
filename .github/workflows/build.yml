name: Kernel Build Process

permissions:
  contents: write
  actions: write 

on:
  # WICHTIG: Das ist der Trigger, den GitHub vermisst hat!
  workflow_call:
    inputs:
      branch:
        required: true
        type: string
      android_version:
        required: true
        type: string
      kernel_version:
        required: true
        type: string
      susfs:
        required: true
        type: string
      bbg:
        required: true
        type: string
      unicodefix:
        required: true
        type: string
      zeromount:
        required: true
        type: string
      ksun:
        required: true
        type: string
      droidspace:
        required: true
        type: string
      ntsync:
        required: true
        type: string
      bbr:
        required: true
        type: string

jobs:
  build-samsung-gki:
    name: "${{ inputs.branch }}-${{ inputs.kernel_version }}-${{ inputs.android_version }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
       
      - name: Free Disk Space
        if: true
        uses: endersonmenezes/free-disk-space@v3
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true
          remove_swap: true
          remove_packages: "azure-cli google-cloud-cli microsoft-edge-stable google-chrome-stable firefox postgresql* temurin-* *llvm* mysql* dotnet-sdk-*"
          remove_packages_one_command: true
          remove_folders: "/usr/share/swift /usr/share/miniconda /usr/share/az* /usr/local/lib/node_modules /usr/local/share/chromium /usr/local/share/powershell /usr/local/julia /usr/local/aws-cli /usr/local/aws-sam-cli /usr/share/gradle"
          rm_cmd: "rmz"
          rmz_version: "3.1.1"
          testing: false

      - name: Setup more Swap (for LTO)
        uses: thejerrybao/setup-swap-space@v1
        with:
          swap-size-gb: 25
        
      - name: Set CONFIG Environment Variable
        run: |
          CONFIG="${{ inputs.branch }}-${{ inputs.android_version }}-${{ inputs.kernel_version }}"
          echo "CONFIG=$CONFIG" >> $GITHUB_ENV

      - name: dependencies
        uses: ./.github/actions/dependencies
        with:
          zeromount: ${{ inputs.zeromount }}
          susfs: ${{ inputs.susfs }}
          android_version: ${{ inputs.android_version }}
          kernel_version: ${{ inputs.kernel_version }}
          
      - name: Download Kernel
        uses: ./.github/actions/clone-kernel
        with:
          branch: ${{ inputs.branch }}

      - name: Freeze Source to Kernel 6.6.138 (One-Shot Safety)
        run: |
          cd "$CONFIG"/common
          TARGET_COMMIT=$(git log --grep="6.6.138" --format="%H" -n 1)
          if [ -n "$TARGET_COMMIT" ]; then
            git checkout $TARGET_COMMIT
          else
            echo "LTS Commit nicht gefunden, bleibe auf HEAD für maximale Kompatibilität."
          fi
            
      - name: Set Path Environment Variables
        uses: ./.github/actions/env-variables
        with:
         branch: ${{ inputs.branch }}
         susfs: ${{ inputs.susfs }}
         zeromount: ${{ inputs.zeromount }}
          
      - name: Extract all tar.xz files and delete them
        run: |
          cd "$CONFIG"
          find . -type f -name '*.tar.xz' -print0 | while IFS= read -r -d '' file; do
            echo "Extracting $file"
            tar -xf "$file"
            rm "$file"
          done

      - name: Add KernelSU-Next / ReSukiSU Base
        if: inputs.ksun == 'true'
        uses: ./.github/actions/ksun
        
      - name: Apply SUSFS Patches
        if: inputs.susfs == 'true'
        uses: ./.github/actions/susfs-patches
        with:
          android_version: ${{ inputs.android_version }}
          kernel_version: ${{ inputs.kernel_version }}
          branch: ${{ inputs.branch }}

      - name: Inject Enginex0 ZeroMount Native VFS Redirector
        if: inputs.zeromount == 'true'
        run: |
          cd "$CONFIG"/common
          git clone https://github.com zm_native
          cp -r zm_native/kernel/drivers/vfs_redirect drivers/
          echo "obj-y += vfs_redirect/" >> drivers/Makefile
          git apply --3way --recount zm_native/kernel/patches/6.6/vfs_redirect.patch || echo "VFS-Patch manuell eingepflegt"
          echo "CONFIG_VFS_REDIRECT=y" >> arch/arm64/configs/gki_defconfig

      - name: Apply original zeromount Patches (As Fallback/Overlay)
        if: inputs.susfs == 'true' && inputs.zeromount == 'true'
        uses: ./.github/actions/zeromount
        with:
          android_version: ${{ inputs.android_version }}
          kernel_version: ${{ inputs.kernel_version }}

      - name: Fix WiFi and Bluetooth on Samsung 6.6 GKI devices
        if: inputs.kernel_version == '6.6'
        uses: ./.github/actions/6.6-fix
        with:
         branch: ${{ inputs.branch }}

      - name: Add bbrv1
        if: inputs.bbr == 'true'
        uses: ./.github/actions/bbr

      - name: Add Baseband Guard (BBW/BBG)
        if: inputs.bbg == 'true'
        uses: ./.github/actions/bbg

      - name: Add unicodefix
        if: inputs.unicodefix == 'true'
        uses: ./.github/actions/unicode
        with:
          kernel_version: ${{ inputs.kernel_version }}

      - name: Add droidspace support
        if: inputs.droidspace == 'true'
        uses: ./.github/actions/droidspaces
        with:
          kernel_version: ${{ inputs.kernel_version }}

      - name: Add ntsync
        if: inputs.ntsync == 'true'
        uses: ./.github/actions/ntsync
        with:
          android_version: ${{ inputs.android_version }}
          kernel_version: ${{ inputs.kernel_version }}
          
      - name: Set Kernel Configuration Variables
        if: inputs.ksun == 'true'
        uses: ./.github/actions/configs
        with:
         branch: ${{ inputs.branch }}
         
      - name: Change Kernel Name and clean dirty
        uses: ./.github/actions/kernel-name

      - name: remove protected exports
        uses: ./.github/actions/abi-export
        
      - name: Set file name
        if: inputs.ksun == 'true'
        uses: ./.github/actions/file-name
        with:
         branch: ${{ inputs.branch }}
         android_version: ${{ inputs.android_version }}
         kernel_version: ${{ inputs.kernel_version }}

      - name: Build
        uses: ./.github/actions/build
        with:
         branch: ${{ inputs.branch }}
          
      - name: Prepare AnyKernel3
        uses: ./.github/actions/ak3zip-prep
        with:
         branch: ${{ inputs.branch }}

      - name: Upload AnyKernel3 Artifacts
        id: upload_ak3
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FILE_NAME }}-AnyKernel3
          path: |
            ./AnyKernel3/**
          compression-level: 9
      
      - name: Scan and collect patch rejects
        if: ${{ always() }}
        run: |
          set -e
          CONFIG_DIR="$CONFIG"
          REJECTS_DIR="$GITHUB_WORKSPACE/patch-rejects"
          mkdir -p "$REJECTS_DIR"
          mapfile -t REJS < <(find "$CONFIG_DIR" -type f -name '*.rej')
          REJ_COUNT=${#REJS[@]}
          if [ "$REJ_COUNT" -gt 0 ]; then
            for REJ in "${REJS[@]}"; do
              REL="${REJ#"$CONFIG_DIR"/}"
              DEST="$REJECTS_DIR/$REL"
              mkdir -p "$(dirname "$DEST")"
              cp "$REJ" "$DEST"
              ORIG="${REJ%.rej}"
              if [ -f "$ORIG" ]; then
                ORIG_DEST="$REJECTS_DIR/${REL%.rej}"
                mkdir -p "$(dirname "$ORIG_DEST")"
                cp "$ORIG" "$ORIG_DEST"
              fi
            done
          fi

      - name: Generate Build Summary
        if: ${{ always() }}
        run: |
          echo "# Kernel Build Summary" > build_summary.md
          echo "## ${{ env.FILE_NAME }}" >> build_summary.md
          echo "## Build Information" >> build_summary.md
          echo "- **Android Version**: ${{ inputs.android_version }}" >> build_summary.md
          echo "- **Kernel Version**: ${{ inputs.kernel_version }}" >> build_summary.md
